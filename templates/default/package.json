{
  "author": "{{user}}",
  "name": "{{name}}",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/{{user}}/{{name}}"
  },
  "scripts": {
    "test": "cake test",
    "start": "cake run"
  },
  "dependencies": {
    "die": "{{dieVersion}}"
  },
  "devDependencies": {},
  "optionalDependencies": {},
  "engines": {
    "node": "*"
  }
}
