{
  "author": "{{user}}",
  "name": "{{name}}",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/{{user}}/{{name}}"
  },
  "main": "index",
  "scripts": {
    "start": "node_modules/.bin/die run",
    "test": "node_modules/.bin/die test"
  },
  "dependencies": {
    "die": "{{dieVersion}}",
    "coffee-script": "*",
    "markdown": "*"
  },
  "devDependencies": {
    "chai": "*",
    "mocha": "*",
    "supertest": "*"
  },
  "engines": {
    "node": "*"
  }
}
