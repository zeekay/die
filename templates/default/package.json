{
  "author": "{{user}}",
  "name": "{{name}}",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/{{user}}/{{name}}"
  },
  "main": "index",
  "scripts": {
    "start": "die run",
    "test": "die test"
  },
  "dependencies": {
    "die": "{{dieVersion}}"
  },
  "devDependencies": {
    "chai": "~1.0.4",
    "mocha": "~1.2.0",
    "zombie": "~1.3.1"
  },
  "optionalDependencies": {},
  "engines": {
    "node": "*"
  }
}
