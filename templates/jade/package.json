{
  "author": "{{user}}",
  "name": "{{name}}",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/{{user}}/{{name}}"
  },
  "main": "index",
  "scripts": {
    "start": "die run",
    "test": "die test"
  },
  "dependencies": {
    "die": "{{dieVersion}}"
  },
  "devDependencies": {},
  "optionalDependencies": {},
  "engines": {
    "node": "*"
  }
}
