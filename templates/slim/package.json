{
  "author": "{{user}}",
  "name": "{{name}}",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/{{user}}/{{name}}"
  },
  "main": "index",
  "scripts": {
    "start": "node_modules/.bin/die run",
    "test": "node_modules/.bin/die test"
  },
  "dependencies": {
    "die": "{{dieVersion}}",
    "coffee-script": "~1.3.3",
    "markdown": "~0.4.0"
  },
  "devDependencies": {
    "chai": "~1.0.4",
    "mocha": "~1.2.0",
    "zombie": "~1.3.1"
  },
  "engines": {
    "node": "*"
  }
}
