// Generated by CoffeeScript 1.3.3
var fs, path, wrench;

path = require('path');

wrench = require('wrench');

fs = require('fs');

exports.readConfig = function(config) {
  var configPath, key, value;
  if (config == null) {
    config = 'defaults';
  }
  configPath = path.join(process.cwd(), this.options.configPath, config);
  try {
    config = require(configPath);
    for (key in config) {
      value = config[key];
      this.options[key] = value;
    }
  } catch (error) {

  }
  this.options.paths.concat([path.dirname(this.options.main)]);
  return this.options;
};
